import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mainBlack: "#27272a",
        secondaryBlack: "#18181b",
        mainWhite: "#f5f5f5",
        mainPurple: "#a21caf",
        lightPurple: "#d946ef"
    },
});
